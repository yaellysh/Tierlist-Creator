package view;

import javax.swing.*;
import java.awt.*;

/**
 * A panel containing a label and a text field.
 */
class LabelPanel extends JPanel {
    LabelPanel(JLabel label) {
        this.add(label);
    }
}
